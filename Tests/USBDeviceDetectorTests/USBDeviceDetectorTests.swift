import XCTest
@testable import USBDeviceDetector

final class USBDeviceDetectorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(USBDeviceDetector().text, "Hello, World!")
    }
}
